import { StyleSheet } from "react-native";
import { Colors } from "../../const/colors";

export const messageItemListStyle = StyleSheet.create({
  wrapper: {
    flex: 1,
    backgroundColor: "#fff",
  },
});
